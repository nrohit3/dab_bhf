# This is a Declarative Automation Bundle definition for bhf1_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bhf1_bundle
  uuid: e1b6a1b5-9fef-4460-abe7-6fa045e367bc

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    presets:
      name_prefix: ''
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: production
    default: true
    workspace:
      host: https://dbc-c59b1472-afc6.cloud.databricks.com
      root_path: /Workspace/Shared/bhf1/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dab_catalog_dev
      schema: rnd
    
  prod:
    mode: production
    workspace:
      host: https://dbc-408a3f5f-9a23.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/nrohit3@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/bhf1/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dab_catalog_prod
      schema: rnd
    permissions:
      - user_name: dbvrsn90@gmail.com
        level: CAN_MANAGE
